[phases.setup]
nixPkgs = ["python310", "python310Packages.pip", "curl", "iptables", "jq"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "./start.sh"
